# Backend environment variables (example). Copy to backend/.env and fill values.

# JWT secret for auth tokens
JWT_SECRET=change_me_strong_secret

# OpenAI API key for backend chat integrations (if used server-side)
OPENAI_API_KEY=sk-...

# PostgreSQL connection (if using pg Pool without defaults)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# CORS allowed origins
CORS_ORIGIN=http://localhost:3000
